# Configurações de ambiente para Docker
# Copie este arquivo para .env e ajuste as variáveis conforme necessário

# Ambiente da aplicação (development, production)
NODE_ENV=development

# Configurações da aplicação React
REACT_APP_SKIP_AUTH=true
REACT_APP_GOOGLE_CLIENT_ID=your_google_client_id_here
REACT_APP_API_URL=http://localhost:8080

# Configurações do Docker
COMPOSE_PROJECT_NAME=price-guard

# Portas personalizadas (opcional)
DEV_PORT=3000
PROD_HTTP_PORT=80
PROD_HTTPS_PORT=443
NGINX_DEV_PORT=8080
